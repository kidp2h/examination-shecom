---
version: '3.9'
services:
  db:
    image: postgres
    volumes: [data:/var/lib/postgresql/data]
    environment:
      POSTGRES_USER: local
      POSTGRES_PASSWORD: local
      POSTGRES_DB: local
    ports: [5432:5432]
volumes:
  data:
